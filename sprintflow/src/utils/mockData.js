// Mock data removed — all data is now served from the MongoDB backend.
// These empty exports are kept to avoid breaking any accidental imports.
export const MOCK_PROJECTS = []
export const MOCK_TASKS = []
